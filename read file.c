# Genomigo_C
#include<stdio.h>
#include<stdlib.h>
int main()
{
file = fileptr;
fptr = fopen("users.csv","r");
if(file == NULL)
{
printf("error");
return 1;
}
while(!foo (fptr))
{
 fgets(line,fptr);
 puts(line);
 }
 fclose(fptr);
 return 0;
 }
